services:
  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "15672:15672"
      - "5672:5672"
    networks:
      - rabbit
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 30s
      timeout: 30s
      retries: 3
  consumer:
    build: HyyConsumer/.
    networks:
      - rabbit
    deploy:
      mode: replicated
      replicas: 10
    depends_on:
      rabbitmq:
        condition: service_healthy
        restart: true
  producer:
    build: HyyProducer/.
    networks:
      - rabbit
    deploy:
      mode: replicated
      replicas: 1
    depends_on:
      rabbitmq:
        condition: service_healthy
        restart: true

networks:
  rabbit:
    driver: overlay
    attachable: true

